# 📚 선생님용 관리 가이드

이 문서는 **코딩 지식 없이** 문제를 추가·수정하는 방법을 설명합니다.

---

## 한눈에 보기

- 문제와 단원은 모두 **`src/data/` 폴더** 안에 있습니다. 이 폴더만 편집하면 됩니다.
- GitHub 웹사이트에서 파일을 수정하고 저장(Commit)하면, **Vercel이 1~2분 안에 자동으로 새 버전을 배포**합니다. 별도의 프로그램 설치나 명령어 입력이 필요 없습니다.

```
src/data/
├── units.ts                  ← 단원 목록 (단원 추가/숨기기)
└── questions/
    ├── index.ts              ← 문제 파일 등록부
    ├── phoneme-basics.ts     ← [음운의 개념과 환경] 문제
    ├── consonant-system.ts   ← [자음 체계] 문제
    ├── final-consonants.ts   ← [받침의 발음] 문제
    └── morpheme-basics.ts    ← [형태소·조사·어미] 문제
```

---

## 1. 기존 문제 수정하기 (가장 자주 하는 일)

1. GitHub 저장소에서 `src/data/questions/` 폴더로 이동
2. 수정할 단원 파일 클릭 → 오른쪽 위 **연필 아이콘(✏️ Edit)** 클릭
3. 문제 내용 수정
4. 오른쪽 위 **Commit changes** 버튼 클릭 → 완료!

### 문제 하나의 모양

```ts
{
  id: 'PB001',                  // 문제 번호 (겹치면 안 됨)
  unitCode: 'phoneme-basics',   // 단원 코드 (units.ts와 동일하게)
  type: '객관식',                // 객관식 | ox | 빈칸 | 단답형
  difficulty: '하',              // 상 | 중 | 하
  passage: '',                  // <보기> 지문 (없으면 '' 그대로)
  question: '문제 내용?',
  choices: ['보기1', '보기2', '보기3', '보기4'],  // 객관식만 채움
  answer: '2',                  // 정답 (아래 표 참고)
  explanation: '해설 내용',      // 틀렸을 때 학생에게 보여줌
},
```

### 유형별 정답(answer) 쓰는 법

| 유형 | answer 형식 | 예시 |
|------|------------|------|
| 객관식 | 보기 번호 | `'2'` (2번이 정답) |
| ox | O 또는 X | `'O'` |
| 빈칸 | 빈칸 여러 개면 `/`로 구분 | `'어간/어미'` (문제에 `[___]` 2개) |
| 단답형 | 정답 텍스트, 복수 정답은 `\|` | `'경음\|된소리'` (둘 다 정답 인정) |

### ⚠️ 주의사항 (오류 방지)

- 문장 안에 **작은따옴표(')가 들어가면** 전체를 큰따옴표(")로 감싸세요.
  - ✅ `question: "'국물'에서 'ㅁ' 앞에 놓인 것은?"`
- 각 항목 끝의 **쉼표(,)를 지우지 마세요.**
- `{` `}` `[` `]` 괄호를 실수로 지우지 않도록 주의하세요.
- 저장 후 Vercel 배포가 실패하면(이메일 알림 옴) 대부분 따옴표/쉼표 문제입니다. 방금 수정한 부분을 다시 확인하세요.

---

## 2. 문제 추가하기

수정과 같은 방법으로 파일을 열고, 기존 문제 하나를 **통째로 복사**해서 마지막 문제 뒤에 붙여 넣은 다음 내용만 바꾸면 됩니다. `id`는 겹치지 않게 번호를 올려 주세요 (예: `PB013` → `PB014`).

---

## 3. 새 단원 추가하기 (새 수업 진도 나갈 때)

1. **`src/data/units.ts`** 열고, 기존 단원 하나를 복사해 추가:
   ```ts
   {
     code: 'phoneme-change',        // 새 영문 코드 (겹치면 안 됨)
     name: '음운의 변동',
     description: '비음화, 유음화 등 음운 변동을 학습합니다.',
     order: 5,                      // 표시 순서
     active: true,
   },
   ```
2. **`src/data/questions/`** 폴더에 새 파일 만들기 (예: `phoneme-change.ts`)
   - 기존 파일(예: `phoneme-basics.ts`)을 열어 전체 복사 → 새 파일에 붙여넣기 → 변수 이름과 문제 내용 수정
   - GitHub에서 새 파일 만들기: 폴더 화면에서 **Add file → Create new file**
3. **`src/data/questions/index.ts`** 열고 두 줄 추가:
   ```ts
   import { phonemeChangeQuestions } from './phoneme-change';   // ① import 추가

   export const allQuestions: Question[] = [
     ...phonemeBasicsQuestions,
     ...phonemeChangeQuestions,                                  // ② 배열에 추가
   ];
   ```

---

## 4. 단원 숨기기 (시험 기간 등)

`src/data/units.ts`에서 해당 단원의 `active: true`를 `active: false`로 바꾸면 화면에서 사라집니다. 문제는 지워지지 않으므로 다시 `true`로 바꾸면 복구됩니다.

---

## 5. (선택) Google Sheets로 관리하기

파일 수정이 번거롭다면 Google Sheets로도 관리할 수 있습니다.

1. 구글 스프레드시트를 만들고 시트 2개 생성: `단원목록`, `문제`
   - **단원목록** 열 순서: 단원코드 | 단원명 | 설명 | 순서 | 활성(TRUE/FALSE)
   - **문제** 열 순서: 문제ID | 단원코드 | 유형 | 난이도 | 지문 | 문제 | 보기1 | 보기2 | 보기3 | 보기4 | 정답 | 해설
2. 시트를 **"링크가 있는 모든 사용자 - 뷰어"**로 공유
3. Vercel 프로젝트 설정 → Environment Variables에 `GOOGLE_SHEET_ID` 추가
   (시트 주소 `https://docs.google.com/spreadsheets/d/여기부분/edit`)
4. 시트를 수정하면 **5분 안에** 웹앱에 자동 반영됩니다.

> Google Sheets를 설정하면 `src/data/` 파일 대신 시트 내용이 사용됩니다.
> 시트 설정을 지우면 다시 파일 데이터로 돌아옵니다.

---

## 6. 자주 묻는 질문

**Q. 학생 기록은 어디에 저장되나요?**
각 학생의 휴대폰/PC 브라우저 안에만 저장됩니다(localStorage). 서버에는 아무것도 저장되지 않아 개인정보 걱정이 없습니다. 단, 학생이 브라우저 데이터를 지우면 기록도 사라집니다.

**Q. 배포가 잘 됐는지 어떻게 확인하나요?**
Vercel 대시보드(vercel.com)에 로그인하면 배포 상태가 보입니다. 초록색 Ready면 성공입니다.

**Q. 실수로 파일을 망가뜨렸어요.**
GitHub의 해당 파일 화면에서 **History**를 누르면 이전 버전을 볼 수 있고, 이전 커밋으로 되돌릴 수 있습니다.
